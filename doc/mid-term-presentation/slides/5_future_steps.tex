\section{Future steps} 
\begin{frame}{Future Steps} 
\vspace{0.5cm}
    \textbf{Idea for the final model}
    \footnotesize
    \begin{itemize}
        \item Investigate the differences between frequentist and bayesian variable selection
        \item Model our target variable as a mixture of distributions
        \item Improve areal random effect with spatial association (Conditional AutoRegressive (CAR) PRIOR): provinces that are close to each other tend to share similar characteristics
        \item Use a Dirichlet Process Prior on the coefficients. Instead of assuming that all the provinces share the same relationships with the covariates, this approach lets data group provinces into clusters that have different patterns
    \end{itemize}
    
\end{frame}